# Для npm start: URL бэкенда.
# Для docker compose build используйте корневой .env по примеру ../.env.example.
REACT_APP_API_URL=http://localhost:8080

# WebSocket URL (опционально). По умолчанию выводится из REACT_APP_API_URL.
# REACT_APP_WS_URL=https://lotus.baxic.ru/ws
